# Phase 1: MVP Backtester Development Plan
## Step-by-Step Implementation Guide

**Duration:** 8 Weeks (Months 1-2)  
**Goal:** Build a minimal viable event-driven backtester with single strategy capability

---

## Overview

```
Week 1-2: Project Setup & Data Infrastructure
Week 3-4: Core Event Engine & Data Handler
Week 5-6: Strategy, Portfolio & Execution
Week 7:   Performance Reporting & Integration
Week 8:   Testing, Validation & Documentation
```

---

## Week 1: Project Setup & Environment

### Step 1.1: Initialize Project Structure
**Time:** Day 1-2

```
trading_system/
├── src/
│   ├── __init__.py
│   ├── events/
│   │   ├── __init__.py
│   │   └── event.py
│   ├── data/
│   │   ├── __init__.py
│   │   └── data_handler.py
│   ├── strategy/
│   │   ├── __init__.py
│   │   └── base_strategy.py
│   ├── portfolio/
│   │   ├── __init__.py
│   │   └── portfolio.py
│   ├── execution/
│   │   ├── __init__.py
│   │   └── execution_handler.py
│   ├── performance/
│   │   ├── __init__.py
│   │   └── metrics.py
│   └── backtest/
│       ├── __init__.py
│       └── engine.py
├── tests/
│   ├── __init__.py
│   ├── test_events.py
│   ├── test_data_handler.py
│   ├── test_portfolio.py
│   └── test_integration.py
├── data/
│   └── sample/
├── configs/
│   └── backtest_config.yaml
├── notebooks/
│   └── analysis.ipynb
├── scripts/
│   └── run_backtest.py
├── requirements.txt
├── pyproject.toml
├── README.md
└── .gitignore
```

**Tasks:**
- [ ] Create directory structure
- [ ] Initialize git repository
- [ ] Create `.gitignore` (exclude data/, __pycache__/, .env, *.pyc)
- [ ] Set up `pyproject.toml` with project metadata

**Deliverable:** Empty project skeleton with proper structure

---

### Step 1.2: Set Up Development Environment
**Time:** Day 2-3

**Tasks:**
- [ ] Create virtual environment: `python -m venv venv`
- [ ] Create `requirements.txt`:

```
# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Data
yfinance>=0.2.0
sqlalchemy>=2.0.0

# Configuration
pyyaml>=6.0
pydantic>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0
```

- [ ] Install dependencies: `pip install -r requirements.txt`
- [ ] Configure `pyproject.toml` for black, ruff, mypy
- [ ] Set up pre-commit hooks (optional but recommended)

**Deliverable:** Working Python environment with all dependencies

---

### Step 1.3: Create Configuration System
**Time:** Day 3-4

**File: `src/config.py`**
```python
from pydantic import BaseModel
from typing import List, Optional
import yaml

class DataConfig(BaseModel):
    symbols: List[str]
    start_date: str
    end_date: str
    data_source: str = "csv"  # csv, yfinance, database
    data_path: Optional[str] = None

class ExecutionConfig(BaseModel):
    initial_capital: float = 100000.0
    commission_pct: float = 0.001  # 0.1%
    slippage_pct: float = 0.0005  # 0.05%

class StrategyConfig(BaseModel):
    name: str
    parameters: dict = {}

class BacktestConfig(BaseModel):
    data: DataConfig
    execution: ExecutionConfig
    strategy: StrategyConfig

def load_config(path: str) -> BacktestConfig:
    with open(path, 'r') as f:
        data = yaml.safe_load(f)
    return BacktestConfig(**data)
```

**File: `configs/backtest_config.yaml`**
```yaml
data:
  symbols:
    - AAPL
  start_date: "2020-01-01"
  end_date: "2023-12-31"
  data_source: "csv"
  data_path: "data/sample"

execution:
  initial_capital: 100000.0
  commission_pct: 0.001
  slippage_pct: 0.0005

strategy:
  name: "sma_crossover"
  parameters:
    short_window: 20
    long_window: 50
```

**Tasks:**
- [ ] Implement `config.py` with Pydantic models
- [ ] Create sample YAML configuration
- [ ] Write unit test for config loading

**Deliverable:** Working configuration system with validation

---

## Week 2: Data Infrastructure

### Step 2.1: Define Event Classes
**Time:** Day 1-2

**File: `src/events/event.py`**
```python
from dataclasses import dataclass
from datetime import datetime
from enum import Enum
from typing import Optional

class EventType(Enum):
    MARKET = "MARKET"
    SIGNAL = "SIGNAL"
    ORDER = "ORDER"
    FILL = "FILL"

class SignalType(Enum):
    LONG = "LONG"
    SHORT = "SHORT"
    EXIT = "EXIT"

class OrderType(Enum):
    MARKET = "MARKET"
    LIMIT = "LIMIT"

class OrderSide(Enum):
    BUY = "BUY"
    SELL = "SELL"

@dataclass
class Event:
    event_type: EventType
    timestamp: datetime

@dataclass
class MarketEvent(Event):
    """Generated when new market data is available"""
    def __init__(self, timestamp: datetime):
        super().__init__(EventType.MARKET, timestamp)

@dataclass
class SignalEvent(Event):
    """Generated by strategy when trading signal detected"""
    symbol: str
    signal_type: SignalType
    strength: float = 1.0  # Signal strength 0-1
    
    def __init__(self, timestamp: datetime, symbol: str, 
                 signal_type: SignalType, strength: float = 1.0):
        super().__init__(EventType.SIGNAL, timestamp)
        self.symbol = symbol
        self.signal_type = signal_type
        self.strength = strength

@dataclass
class OrderEvent(Event):
    """Generated by portfolio to execute trade"""
    symbol: str
    order_type: OrderType
    side: OrderSide
    quantity: int
    limit_price: Optional[float] = None
    
    def __init__(self, timestamp: datetime, symbol: str,
                 order_type: OrderType, side: OrderSide, 
                 quantity: int, limit_price: Optional[float] = None):
        super().__init__(EventType.ORDER, timestamp)
        self.symbol = symbol
        self.order_type = order_type
        self.side = side
        self.quantity = quantity
        self.limit_price = limit_price

@dataclass
class FillEvent(Event):
    """Generated by execution handler after order filled"""
    symbol: str
    side: OrderSide
    quantity: int
    fill_price: float
    commission: float
    
    def __init__(self, timestamp: datetime, symbol: str,
                 side: OrderSide, quantity: int, 
                 fill_price: float, commission: float):
        super().__init__(EventType.FILL, timestamp)
        self.symbol = symbol
        self.side = side
        self.quantity = quantity
        self.fill_price = fill_price
        self.commission = commission
```

**Tasks:**
- [ ] Implement all event classes
- [ ] Write unit tests for event creation
- [ ] Verify dataclass serialization works

**Deliverable:** Complete event type system

---

### Step 2.2: Implement Data Handler
**Time:** Day 2-4

**File: `src/data/data_handler.py`**
```python
from abc import ABC, abstractmethod
from typing import Dict, List, Optional, Tuple
from datetime import datetime
import pandas as pd
import numpy as np

class DataHandler(ABC):
    """Abstract base class for data handlers"""
    
    @abstractmethod
    def get_latest_bars(self, symbol: str, n: int = 1) -> pd.DataFrame:
        """Return last N bars for symbol (NEVER future data)"""
        raise NotImplementedError
    
    @abstractmethod
    def update_bars(self) -> bool:
        """Advance to next bar. Returns False when data exhausted."""
        raise NotImplementedError
    
    @abstractmethod
    def get_current_timestamp(self) -> datetime:
        """Return current simulation timestamp"""
        raise NotImplementedError
    
    @property
    @abstractmethod
    def continue_backtest(self) -> bool:
        """Check if more data available"""
        raise NotImplementedError


class HistoricalCSVDataHandler(DataHandler):
    """
    Data handler for CSV files with OHLCV data.
    
    CRITICAL: This class enforces no look-ahead bias by only
    exposing data up to current_index.
    """
    
    def __init__(self, data_path: str, symbols: List[str],
                 start_date: str, end_date: str):
        self.data_path = data_path
        self.symbols = symbols
        self.start_date = pd.Timestamp(start_date)
        self.end_date = pd.Timestamp(end_date)
        
        # Storage for each symbol's data
        self.data: Dict[str, pd.DataFrame] = {}
        self.current_index: int = 0
        self.max_index: int = 0
        
        # Load all data
        self._load_data()
    
    def _load_data(self):
        """Load CSV data for all symbols"""
        for symbol in self.symbols:
            filepath = f"{self.data_path}/{symbol}.csv"
            
            df = pd.read_csv(filepath, parse_dates=['date'], index_col='date')
            
            # Filter to date range
            df = df[(df.index >= self.start_date) & 
                    (df.index <= self.end_date)]
            
            # Ensure columns are lowercase
            df.columns = df.columns.str.lower()
            
            # Required columns check
            required = ['open', 'high', 'low', 'close', 'volume']
            missing = [c for c in required if c not in df.columns]
            if missing:
                raise ValueError(f"Missing columns for {symbol}: {missing}")
            
            df = df.sort_index()
            self.data[symbol] = df
            
            # Track max length
            if len(df) > self.max_index:
                self.max_index = len(df)
        
        print(f"Loaded {len(self.symbols)} symbols, "
              f"{self.max_index} bars from {self.start_date} to {self.end_date}")
    
    def get_latest_bars(self, symbol: str, n: int = 1) -> pd.DataFrame:
        """
        Return the last N bars for a symbol.
        
        CRITICAL: Only returns data up to current_index to prevent
        look-ahead bias. This is the ONLY way strategies should
        access market data.
        """
        if symbol not in self.data:
            raise KeyError(f"Symbol {symbol} not found in data")
        
        df = self.data[symbol]
        
        # Only return data up to current position (inclusive)
        available = df.iloc[:self.current_index + 1]
        
        # Return last N bars
        return available.tail(n).copy()
    
    def update_bars(self) -> bool:
        """
        Advance to next bar.
        Returns True if successful, False if data exhausted.
        """
        self.current_index += 1
        return self.current_index < self.max_index
    
    def get_current_timestamp(self) -> datetime:
        """Return timestamp of current bar"""
        # Use first symbol's index as reference
        first_symbol = self.symbols[0]
        df = self.data[first_symbol]
        
        if self.current_index < len(df):
            return df.index[self.current_index].to_pydatetime()
        return df.index[-1].to_pydatetime()
    
    @property
    def continue_backtest(self) -> bool:
        """Check if more data is available"""
        return self.current_index < self.max_index - 1
    
    def get_all_symbols(self) -> List[str]:
        """Return list of available symbols"""
        return self.symbols.copy()
```

**Tasks:**
- [ ] Implement abstract DataHandler base class
- [ ] Implement HistoricalCSVDataHandler
- [ ] Create sample CSV data file for testing
- [ ] Write unit tests for data handler
- [ ] Verify no look-ahead bias in get_latest_bars

**Deliverable:** Working data handler with CSV support

---

### Step 2.3: Create Sample Data & Data Download Script
**Time:** Day 4-5

**File: `scripts/download_data.py`**
```python
"""Download historical data for backtesting"""
import yfinance as yf
import pandas as pd
from pathlib import Path
import argparse

def download_symbol(symbol: str, start: str, end: str, 
                    output_dir: str) -> bool:
    """Download OHLCV data for a single symbol"""
    try:
        ticker = yf.Ticker(symbol)
        df = ticker.history(start=start, end=end)
        
        if df.empty:
            print(f"No data found for {symbol}")
            return False
        
        # Standardize columns
        df = df.reset_index()
        df.columns = df.columns.str.lower()
        df = df.rename(columns={'date': 'date'})
        
        # Keep only OHLCV
        df = df[['date', 'open', 'high', 'low', 'close', 'volume']]
        
        # Save to CSV
        output_path = Path(output_dir) / f"{symbol}.csv"
        df.to_csv(output_path, index=False)
        
        print(f"Downloaded {symbol}: {len(df)} bars")
        return True
        
    except Exception as e:
        print(f"Error downloading {symbol}: {e}")
        return False

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument('--symbols', nargs='+', 
                        default=['AAPL', 'MSFT', 'GOOGL'])
    parser.add_argument('--start', default='2020-01-01')
    parser.add_argument('--end', default='2023-12-31')
    parser.add_argument('--output', default='data/sample')
    args = parser.parse_args()
    
    Path(args.output).mkdir(parents=True, exist_ok=True)
    
    for symbol in args.symbols:
        download_symbol(symbol, args.start, args.end, args.output)

if __name__ == '__main__':
    main()
```

**Tasks:**
- [ ] Create data download script
- [ ] Download sample data (AAPL, MSFT, GOOGL)
- [ ] Verify CSV format is correct
- [ ] Test data handler with downloaded data

**Deliverable:** Sample data files and download utility

---

## Week 3: Core Event Engine

### Step 3.1: Implement Event Queue
**Time:** Day 1-2

**File: `src/events/queue.py`**
```python
from queue import Queue, Empty
from typing import Optional
from .event import Event

class EventQueue:
    """
    FIFO event queue for the backtesting engine.
    
    All components communicate through this queue:
    - DataHandler -> MarketEvent
    - Strategy -> SignalEvent  
    - Portfolio -> OrderEvent
    - ExecutionHandler -> FillEvent
    """
    
    def __init__(self):
        self._queue: Queue = Queue()
        self._event_count: int = 0
    
    def put(self, event: Event) -> None:
        """Add event to queue"""
        self._queue.put(event)
        self._event_count += 1
    
    def get(self, block: bool = False, 
            timeout: Optional[float] = None) -> Optional[Event]:
        """
        Get next event from queue.
        
        Args:
            block: If True, wait for event
            timeout: Max seconds to wait if blocking
            
        Returns:
            Event or None if queue empty (non-blocking)
            
        Raises:
            Empty: If blocking and timeout exceeded
        """
        try:
            return self._queue.get(block=block, timeout=timeout)
        except Empty:
            return None
    
    def empty(self) -> bool:
        """Check if queue is empty"""
        return self._queue.empty()
    
    @property
    def event_count(self) -> int:
        """Total events processed"""
        return self._event_count
    
    def clear(self) -> None:
        """Clear all pending events"""
        while not self._queue.empty():
            try:
                self._queue.get_nowait()
            except Empty:
                break
```

**Tasks:**
- [ ] Implement EventQueue class
- [ ] Write unit tests for queue operations
- [ ] Test thread safety (optional for MVP)

**Deliverable:** Working event queue

---

### Step 3.2: Implement Backtest Engine Core
**Time:** Day 2-4

**File: `src/backtest/engine.py`**
```python
from typing import Optional
from datetime import datetime

from ..events.queue import EventQueue
from ..events.event import (
    Event, EventType, MarketEvent, SignalEvent, 
    OrderEvent, FillEvent
)
from ..data.data_handler import DataHandler
from ..strategy.base_strategy import Strategy
from ..portfolio.portfolio import Portfolio
from ..execution.execution_handler import ExecutionHandler
from ..performance.metrics import PerformanceTracker

class BacktestEngine:
    """
    Main backtesting engine implementing event-driven simulation.
    
    Event Flow:
    1. DataHandler emits MarketEvent
    2. Strategy receives MarketEvent, may emit SignalEvent
    3. Portfolio receives SignalEvent, may emit OrderEvent
    4. ExecutionHandler receives OrderEvent, emits FillEvent
    5. Portfolio receives FillEvent, updates positions
    
    Loop continues until data exhausted.
    """
    
    def __init__(
        self,
        data_handler: DataHandler,
        strategy: 'Strategy',
        portfolio: Portfolio,
        execution_handler: ExecutionHandler,
        performance_tracker: Optional[PerformanceTracker] = None
    ):
        self.data_handler = data_handler
        self.strategy = strategy
        self.portfolio = portfolio
        self.execution_handler = execution_handler
        self.performance_tracker = performance_tracker
        
        self.events = EventQueue()
        
        # Wire up components to event queue
        self.strategy.set_event_queue(self.events)
        self.portfolio.set_event_queue(self.events)
        self.execution_handler.set_event_queue(self.events)
        
        # Statistics
        self.bar_count = 0
        self.event_count = 0
    
    def run(self) -> dict:
        """
        Execute backtest simulation.
        
        Returns:
            Dictionary with performance results
        """
        print("Starting backtest...")
        start_time = datetime.now()
        
        # Main loop - iterate through each bar
        while self.data_handler.continue_backtest:
            # Advance to next bar
            self.data_handler.update_bars()
            self.bar_count += 1
            
            # Get current timestamp
            timestamp = self.data_handler.get_current_timestamp()
            
            # Emit market event
            market_event = MarketEvent(timestamp)
            self.events.put(market_event)
            
            # Process all events generated by this bar
            self._process_events()
            
            # Update portfolio valuation
            self.portfolio.update_timeindex(timestamp)
            
            # Track performance
            if self.performance_tracker:
                self.performance_tracker.update(
                    timestamp, 
                    self.portfolio.get_equity()
                )
            
            # Progress update every 500 bars
            if self.bar_count % 500 == 0:
                print(f"Processed {self.bar_count} bars...")
        
        # Finalize
        duration = (datetime.now() - start_time).total_seconds()
        
        print(f"\nBacktest complete:")
        print(f"  Bars processed: {self.bar_count}")
        print(f"  Events processed: {self.event_count}")
        print(f"  Duration: {duration:.2f} seconds")
        
        return self._generate_results()
    
    def _process_events(self) -> None:
        """Process all events in queue until empty"""
        while not self.events.empty():
            event = self.events.get()
            if event is None:
                break
            
            self.event_count += 1
            self._handle_event(event)
    
    def _handle_event(self, event: Event) -> None:
        """Route event to appropriate handler"""
        if event.event_type == EventType.MARKET:
            self.strategy.on_market_data(event, self.data_handler)
            
        elif event.event_type == EventType.SIGNAL:
            self.portfolio.on_signal(event, self.data_handler)
            
        elif event.event_type == EventType.ORDER:
            self.execution_handler.execute_order(event, self.data_handler)
            
        elif event.event_type == EventType.FILL:
            self.portfolio.on_fill(event)
    
    def _generate_results(self) -> dict:
        """Generate final backtest results"""
        results = {
            'bars_processed': self.bar_count,
            'events_processed': self.event_count,
            'final_equity': self.portfolio.get_equity(),
            'positions': self.portfolio.get_positions(),
            'trade_history': self.portfolio.get_trade_history(),
        }
        
        if self.performance_tracker:
            results['metrics'] = self.performance_tracker.calculate_metrics()
            results['equity_curve'] = self.performance_tracker.get_equity_curve()
        
        return results
```

**Tasks:**
- [ ] Implement BacktestEngine class
- [ ] Implement event routing logic
- [ ] Add progress reporting
- [ ] Write integration test with mock components

**Deliverable:** Working backtest engine core

---

## Week 4: Strategy Base Class

### Step 4.1: Implement Strategy Abstract Class
**Time:** Day 1-2

**File: `src/strategy/base_strategy.py`**
```python
from abc import ABC, abstractmethod
from typing import Dict, Any, Optional

from ..events.queue import EventQueue
from ..events.event import MarketEvent, SignalEvent, SignalType
from ..data.data_handler import DataHandler

class Strategy(ABC):
    """
    Abstract base class for trading strategies.
    
    Subclasses must implement:
    - calculate_signals(): Core signal generation logic
    
    Strategies receive MarketEvents and may emit SignalEvents.
    """
    
    def __init__(self, symbols: list, parameters: Dict[str, Any] = None):
        self.symbols = symbols
        self.parameters = parameters or {}
        self.events: Optional[EventQueue] = None
        
        # Track strategy state
        self.current_positions: Dict[str, int] = {s: 0 for s in symbols}
    
    def set_event_queue(self, events: EventQueue) -> None:
        """Set the event queue for emitting signals"""
        self.events = events
    
    def on_market_data(self, event: MarketEvent, 
                       data_handler: DataHandler) -> None:
        """
        Called when new market data available.
        
        This method fetches data and calls calculate_signals().
        Subclasses should NOT override this method.
        """
        signals = self.calculate_signals(event.timestamp, data_handler)
        
        for signal in signals:
            self.events.put(signal)
    
    @abstractmethod
    def calculate_signals(self, timestamp, 
                         data_handler: DataHandler) -> list:
        """
        Generate trading signals based on current market data.
        
        MUST be implemented by subclasses.
        
        Args:
            timestamp: Current simulation time
            data_handler: Access to market data (use get_latest_bars only!)
            
        Returns:
            List of SignalEvent objects (may be empty)
            
        IMPORTANT: Only use data_handler.get_latest_bars() to access data.
        This ensures no look-ahead bias.
        """
        raise NotImplementedError
    
    def update_position(self, symbol: str, quantity: int) -> None:
        """Update tracked position (called by portfolio)"""
        self.current_positions[symbol] = quantity
    
    def _create_signal(self, timestamp, symbol: str, 
                      signal_type: SignalType, 
                      strength: float = 1.0) -> SignalEvent:
        """Helper to create signal events"""
        return SignalEvent(
            timestamp=timestamp,
            symbol=symbol,
            signal_type=signal_type,
            strength=strength
        )
```

**Tasks:**
- [ ] Implement Strategy abstract base class
- [ ] Define clear interface contract
- [ ] Add position tracking
- [ ] Document look-ahead bias prevention

**Deliverable:** Strategy base class

---

### Step 4.2: Implement SMA Crossover Strategy
**Time:** Day 2-4

**File: `src/strategy/sma_crossover.py`**
```python
from typing import Dict, Any, List
import pandas as pd

from .base_strategy import Strategy
from ..events.event import SignalEvent, SignalType
from ..data.data_handler import DataHandler

class SMACrossoverStrategy(Strategy):
    """
    Simple Moving Average Crossover Strategy.
    
    Generates LONG signal when short SMA crosses above long SMA.
    Generates EXIT signal when short SMA crosses below long SMA.
    
    Parameters:
        short_window: Period for short SMA (default: 20)
        long_window: Period for long SMA (default: 50)
    """
    
    def __init__(self, symbols: list, parameters: Dict[str, Any] = None):
        super().__init__(symbols, parameters)
        
        # Extract parameters with defaults
        self.short_window = self.parameters.get('short_window', 20)
        self.long_window = self.parameters.get('long_window', 50)
        
        # Validate parameters
        if self.short_window >= self.long_window:
            raise ValueError("short_window must be less than long_window")
        
        # Track previous SMA values for crossover detection
        self.prev_short_sma: Dict[str, float] = {}
        self.prev_long_sma: Dict[str, float] = {}
        
        print(f"SMA Crossover initialized: short={self.short_window}, "
              f"long={self.long_window}")
    
    def calculate_signals(self, timestamp, 
                         data_handler: DataHandler) -> List[SignalEvent]:
        """
        Calculate SMA crossover signals.
        
        Logic:
        - If short SMA crosses ABOVE long SMA -> LONG signal
        - If short SMA crosses BELOW long SMA -> EXIT signal
        """
        signals = []
        
        for symbol in self.symbols:
            signal = self._check_crossover(timestamp, symbol, data_handler)
            if signal:
                signals.append(signal)
        
        return signals
    
    def _check_crossover(self, timestamp, symbol: str,
                        data_handler: DataHandler) -> SignalEvent:
        """Check for SMA crossover on a single symbol"""
        
        # Get enough bars to calculate long SMA
        # Need long_window + 1 to detect crossover
        bars = data_handler.get_latest_bars(symbol, self.long_window + 1)
        
        # Not enough data yet
        if len(bars) < self.long_window:
            return None
        
        # Calculate SMAs
        close_prices = bars['close']
        short_sma = close_prices.tail(self.short_window).mean()
        long_sma = close_prices.tail(self.long_window).mean()
        
        # Get previous SMAs (if available)
        prev_short = self.prev_short_sma.get(symbol)
        prev_long = self.prev_long_sma.get(symbol)
        
        # Update stored values
        self.prev_short_sma[symbol] = short_sma
        self.prev_long_sma[symbol] = long_sma
        
        # Need previous values to detect crossover
        if prev_short is None or prev_long is None:
            return None
        
        # Detect crossover
        signal = None
        
        # Bullish crossover: short crosses above long
        if prev_short <= prev_long and short_sma > long_sma:
            # Only signal if not already long
            if self.current_positions.get(symbol, 0) <= 0:
                signal = self._create_signal(
                    timestamp, symbol, SignalType.LONG, strength=1.0
                )
                print(f"[{timestamp}] {symbol}: LONG signal "
                      f"(SMA {self.short_window}={short_sma:.2f} > "
                      f"SMA {self.long_window}={long_sma:.2f})")
        
        # Bearish crossover: short crosses below long
        elif prev_short >= prev_long and short_sma < long_sma:
            # Only signal if currently long
            if self.current_positions.get(symbol, 0) > 0:
                signal = self._create_signal(
                    timestamp, symbol, SignalType.EXIT, strength=1.0
                )
                print(f"[{timestamp}] {symbol}: EXIT signal "
                      f"(SMA {self.short_window}={short_sma:.2f} < "
                      f"SMA {self.long_window}={long_sma:.2f})")
        
        return signal
```

**Tasks:**
- [ ] Implement SMA Crossover strategy
- [ ] Add parameter validation
- [ ] Add crossover detection logic
- [ ] Write unit tests with known data
- [ ] Verify signals are generated correctly

**Deliverable:** Working SMA Crossover strategy

---

## Week 5: Portfolio Management

### Step 5.1: Implement Portfolio Class
**Time:** Day 1-3

**File: `src/portfolio/portfolio.py`**
```python
from typing import Dict, List, Optional
from datetime import datetime
from dataclasses import dataclass, field
import pandas as pd

from ..events.queue import EventQueue
from ..events.event import (
    SignalEvent, OrderEvent, FillEvent,
    SignalType, OrderType, OrderSide
)
from ..data.data_handler import DataHandler

@dataclass
class Position:
    """Represents a position in a single symbol"""
    symbol: str
    quantity: int = 0
    avg_cost: float = 0.0
    market_value: float = 0.0
    unrealized_pnl: float = 0.0
    realized_pnl: float = 0.0

@dataclass
class Trade:
    """Record of a completed trade"""
    timestamp: datetime
    symbol: str
    side: str
    quantity: int
    price: float
    commission: float
    pnl: float = 0.0

class Portfolio:
    """
    Portfolio management handling position tracking, order generation,
    and P&L calculation.
    
    Receives SignalEvents and generates OrderEvents.
    Receives FillEvents and updates positions.
    """
    
    def __init__(self, initial_capital: float, symbols: List[str],
                 commission_pct: float = 0.001,
                 position_size_pct: float = 0.1):
        """
        Args:
            initial_capital: Starting cash
            symbols: List of tradeable symbols
            commission_pct: Commission as percentage of trade value
            position_size_pct: Target position size as % of equity
        """
        self.initial_capital = initial_capital
        self.cash = initial_capital
        self.commission_pct = commission_pct
        self.position_size_pct = position_size_pct
        
        self.events: Optional[EventQueue] = None
        
        # Positions for each symbol
        self.positions: Dict[str, Position] = {
            s: Position(symbol=s) for s in symbols
        }
        
        # Trade history
        self.trades: List[Trade] = []
        
        # Equity curve tracking
        self.equity_history: List[Dict] = []
        
        # Current prices for valuation
        self.current_prices: Dict[str, float] = {}
    
    def set_event_queue(self, events: EventQueue) -> None:
        """Set event queue for emitting orders"""
        self.events = events
    
    def on_signal(self, signal: SignalEvent, 
                  data_handler: DataHandler) -> None:
        """
        Process signal event and generate orders.
        
        Position sizing: Use fixed percentage of equity.
        """
        symbol = signal.symbol
        signal_type = signal.signal_type
        
        # Get current price
        bars = data_handler.get_latest_bars(symbol, 1)
        if bars.empty:
            return
        
        current_price = bars['close'].iloc[-1]
        self.current_prices[symbol] = current_price
        
        # Calculate order
        order = self._generate_order(signal, current_price)
        
        if order:
            self.events.put(order)
    
    def _generate_order(self, signal: SignalEvent, 
                       current_price: float) -> Optional[OrderEvent]:
        """Generate order from signal"""
        symbol = signal.symbol
        position = self.positions[symbol]
        
        if signal.signal_type == SignalType.LONG:
            # Calculate position size
            equity = self.get_equity()
            target_value = equity * self.position_size_pct
            quantity = int(target_value / current_price)
            
            if quantity <= 0:
                return None
            
            # Check if we have enough cash
            cost = quantity * current_price * (1 + self.commission_pct)
            if cost > self.cash:
                quantity = int(self.cash / (current_price * (1 + self.commission_pct)))
            
            if quantity <= 0:
                return None
            
            return OrderEvent(
                timestamp=signal.timestamp,
                symbol=symbol,
                order_type=OrderType.MARKET,
                side=OrderSide.BUY,
                quantity=quantity
            )
        
        elif signal.signal_type == SignalType.EXIT:
            # Close entire position
            if position.quantity <= 0:
                return None
            
            return OrderEvent(
                timestamp=signal.timestamp,
                symbol=symbol,
                order_type=OrderType.MARKET,
                side=OrderSide.SELL,
                quantity=position.quantity
            )
        
        elif signal.signal_type == SignalType.SHORT:
            # For MVP, we don't support shorting
            print(f"Warning: SHORT signals not supported in MVP")
            return None
        
        return None
    
    def on_fill(self, fill: FillEvent) -> None:
        """
        Process fill event and update position.
        """
        symbol = fill.symbol
        position = self.positions[symbol]
        
        if fill.side == OrderSide.BUY:
            # Update average cost
            total_cost = (position.quantity * position.avg_cost + 
                         fill.quantity * fill.fill_price)
            new_quantity = position.quantity + fill.quantity
            
            position.avg_cost = total_cost / new_quantity if new_quantity > 0 else 0
            position.quantity = new_quantity
            
            # Deduct cash
            self.cash -= (fill.quantity * fill.fill_price + fill.commission)
            
        elif fill.side == OrderSide.SELL:
            # Calculate realized P&L
            pnl = (fill.fill_price - position.avg_cost) * fill.quantity
            pnl -= fill.commission
            
            position.realized_pnl += pnl
            position.quantity -= fill.quantity
            
            # Add cash
            self.cash += (fill.quantity * fill.fill_price - fill.commission)
            
            # Reset avg cost if position closed
            if position.quantity == 0:
                position.avg_cost = 0.0
        
        # Record trade
        trade = Trade(
            timestamp=fill.timestamp,
            symbol=symbol,
            side=fill.side.value,
            quantity=fill.quantity,
            price=fill.fill_price,
            commission=fill.commission,
            pnl=pnl if fill.side == OrderSide.SELL else 0.0
        )
        self.trades.append(trade)
    
    def update_timeindex(self, timestamp: datetime) -> None:
        """Update portfolio valuation at current timestamp"""
        equity = self.get_equity()
        
        self.equity_history.append({
            'timestamp': timestamp,
            'equity': equity,
            'cash': self.cash
        })
    
    def get_equity(self) -> float:
        """Calculate total portfolio equity"""
        positions_value = sum(
            p.quantity * self.current_prices.get(p.symbol, p.avg_cost)
            for p in self.positions.values()
        )
        return self.cash + positions_value
    
    def get_positions(self) -> Dict[str, Position]:
        """Return current positions"""
        return self.positions.copy()
    
    def get_trade_history(self) -> List[Trade]:
        """Return trade history"""
        return self.trades.copy()
    
    def get_equity_curve(self) -> pd.DataFrame:
        """Return equity curve as DataFrame"""
        return pd.DataFrame(self.equity_history)
```

**Tasks:**
- [ ] Implement Portfolio class
- [ ] Implement position tracking
- [ ] Implement order generation from signals
- [ ] Implement fill processing
- [ ] Add equity tracking
- [ ] Write unit tests

**Deliverable:** Working portfolio management

---

## Week 6: Execution Handler

### Step 6.1: Implement Simulated Execution
**Time:** Day 1-3

**File: `src/execution/execution_handler.py`**
```python
from typing import Optional
from datetime import datetime

from ..events.queue import EventQueue
from ..events.event import OrderEvent, FillEvent, OrderSide, OrderType
from ..data.data_handler import DataHandler

class ExecutionHandler:
    """
    Simulated execution handler for backtesting.
    
    Simulates order fills with configurable:
    - Commission (percentage of trade value)
    - Slippage (percentage of price)
    
    For MVP: All market orders fill at close price + slippage.
    Limit orders are not supported in MVP.
    """
    
    def __init__(self, commission_pct: float = 0.001,
                 slippage_pct: float = 0.0005):
        """
        Args:
            commission_pct: Commission as % of trade value (e.g., 0.001 = 0.1%)
            slippage_pct: Slippage as % of price (e.g., 0.0005 = 0.05%)
        """
        self.commission_pct = commission_pct
        self.slippage_pct = slippage_pct
        self.events: Optional[EventQueue] = None
        
        # Statistics
        self.orders_processed = 0
        self.total_commission = 0.0
        self.total_slippage_cost = 0.0
    
    def set_event_queue(self, events: EventQueue) -> None:
        """Set event queue for emitting fills"""
        self.events = events
    
    def execute_order(self, order: OrderEvent, 
                     data_handler: DataHandler) -> None:
        """
        Execute an order and emit FillEvent.
        
        For backtesting, we assume:
        - Market orders always fill
        - Fill at close price + slippage
        - No partial fills
        """
        if order.order_type == OrderType.LIMIT:
            print(f"Warning: LIMIT orders not supported in MVP, "
                  f"treating as MARKET")
        
        # Get current price
        bars = data_handler.get_latest_bars(order.symbol, 1)
        if bars.empty:
            print(f"Warning: No data for {order.symbol}, order rejected")
            return
        
        base_price = bars['close'].iloc[-1]
        
        # Calculate fill price with slippage
        fill_price = self._apply_slippage(base_price, order.side)
        
        # Calculate commission
        trade_value = fill_price * order.quantity
        commission = trade_value * self.commission_pct
        
        # Track costs
        self.total_commission += commission
        slippage_cost = abs(fill_price - base_price) * order.quantity
        self.total_slippage_cost += slippage_cost
        
        # Create fill event
        fill = FillEvent(
            timestamp=order.timestamp,
            symbol=order.symbol,
            side=order.side,
            quantity=order.quantity,
            fill_price=fill_price,
            commission=commission
        )
        
        self.orders_processed += 1
        self.events.put(fill)
        
        print(f"[{order.timestamp}] FILL: {order.side.value} "
              f"{order.quantity} {order.symbol} @ {fill_price:.2f} "
              f"(commission: ${commission:.2f})")
    
    def _apply_slippage(self, price: float, side: OrderSide) -> float:
        """
        Apply slippage to price.
        
        BUY orders: price increases (worse fill)
        SELL orders: price decreases (worse fill)
        """
        if side == OrderSide.BUY:
            return price * (1 + self.slippage_pct)
        else:
            return price * (1 - self.slippage_pct)
    
    def get_statistics(self) -> dict:
        """Return execution statistics"""
        return {
            'orders_processed': self.orders_processed,
            'total_commission': self.total_commission,
            'total_slippage_cost': self.total_slippage_cost
        }
```

**Tasks:**
- [ ] Implement ExecutionHandler class
- [ ] Add slippage modeling
- [ ] Add commission calculation
- [ ] Track execution statistics
- [ ] Write unit tests

**Deliverable:** Working simulated execution

---

## Week 7: Performance Reporting

### Step 7.1: Implement Performance Metrics
**Time:** Day 1-3

**File: `src/performance/metrics.py`**
```python
from typing import List, Dict, Optional
from datetime import datetime
import pandas as pd
import numpy as np

class PerformanceTracker:
    """
    Tracks and calculates performance metrics.
    
    Metrics calculated:
    - Total Return
    - Annualized Return
    - Sharpe Ratio
    - Sortino Ratio
    - Maximum Drawdown
    - Win Rate
    - Profit Factor
    """
    
    def __init__(self, initial_capital: float, 
                 risk_free_rate: float = 0.02,
                 periods_per_year: int = 252):
        self.initial_capital = initial_capital
        self.risk_free_rate = risk_free_rate
        self.periods_per_year = periods_per_year
        
        self.equity_curve: List[Dict] = []
    
    def update(self, timestamp: datetime, equity: float) -> None:
        """Record equity value at timestamp"""
        self.equity_curve.append({
            'timestamp': timestamp,
            'equity': equity
        })
    
    def get_equity_curve(self) -> pd.DataFrame:
        """Return equity curve as DataFrame"""
        return pd.DataFrame(self.equity_curve)
    
    def calculate_metrics(self) -> Dict:
        """Calculate all performance metrics"""
        if len(self.equity_curve) < 2:
            return {'error': 'Insufficient data'}
        
        df = self.get_equity_curve()
        equity = df['equity']
        
        # Calculate returns
        returns = equity.pct_change().dropna()
        
        metrics = {
            'initial_capital': self.initial_capital,
            'final_equity': equity.iloc[-1],
            'total_return_pct': self._total_return(equity),
            'annualized_return_pct': self._annualized_return(returns),
            'sharpe_ratio': self._sharpe_ratio(returns),
            'sortino_ratio': self._sortino_ratio(returns),
            'max_drawdown_pct': self._max_drawdown(equity),
            'volatility_pct': self._volatility(returns),
            'calmar_ratio': self._calmar_ratio(returns, equity),
            'trading_days': len(equity),
        }
        
        return metrics
    
    def _total_return(self, equity: pd.Series) -> float:
        """Total return as percentage"""
        return ((equity.iloc[-1] / self.initial_capital) - 1) * 100
    
    def _annualized_return(self, returns: pd.Series) -> float:
        """Annualized return as percentage"""
        if len(returns) == 0:
            return 0.0
        total_return = (1 + returns).prod() - 1
        n_years = len(returns) / self.periods_per_year
        if n_years <= 0:
            return 0.0
        annualized = (1 + total_return) ** (1 / n_years) - 1
        return annualized * 100
    
    def _sharpe_ratio(self, returns: pd.Series) -> float:
        """Annualized Sharpe Ratio"""
        if len(returns) == 0 or returns.std() == 0:
            return 0.0
        
        excess_returns = returns - (self.risk_free_rate / self.periods_per_year)
        return (np.sqrt(self.periods_per_year) * 
                excess_returns.mean() / excess_returns.std())
    
    def _sortino_ratio(self, returns: pd.Series) -> float:
        """Sortino Ratio (downside deviation)"""
        if len(returns) == 0:
            return 0.0
        
        excess_returns = returns - (self.risk_free_rate / self.periods_per_year)
        downside_returns = returns[returns < 0]
        
        if len(downside_returns) == 0 or downside_returns.std() == 0:
            return 0.0
        
        downside_std = downside_returns.std()
        return (np.sqrt(self.periods_per_year) * 
                excess_returns.mean() / downside_std)
    
    def _max_drawdown(self, equity: pd.Series) -> float:
        """Maximum drawdown as percentage"""
        running_max = equity.cummax()
        drawdown = (equity - running_max) / running_max
        return drawdown.min() * 100
    
    def _volatility(self, returns: pd.Series) -> float:
        """Annualized volatility as percentage"""
        if len(returns) == 0:
            return 0.0
        return returns.std() * np.sqrt(self.periods_per_year) * 100
    
    def _calmar_ratio(self, returns: pd.Series, 
                     equity: pd.Series) -> float:
        """Calmar Ratio (annual return / max drawdown)"""
        annual_return = self._annualized_return(returns)
        max_dd = abs(self._max_drawdown(equity))
        
        if max_dd == 0:
            return 0.0
        return annual_return / max_dd
    
    def print_report(self, trades: List = None) -> None:
        """Print formatted performance report"""
        metrics = self.calculate_metrics()
        
        print("\n" + "="*60)
        print("BACKTEST PERFORMANCE REPORT")
        print("="*60)
        
        print(f"\nCapital:")
        print(f"  Initial:        ${metrics['initial_capital']:,.2f}")
        print(f"  Final:          ${metrics['final_equity']:,.2f}")
        print(f"  Total Return:   {metrics['total_return_pct']:.2f}%")
        
        print(f"\nRisk-Adjusted Metrics:")
        print(f"  Annualized Return: {metrics['annualized_return_pct']:.2f}%")
        print(f"  Sharpe Ratio:      {metrics['sharpe_ratio']:.2f}")
        print(f"  Sortino Ratio:     {metrics['sortino_ratio']:.2f}")
        print(f"  Calmar Ratio:      {metrics['calmar_ratio']:.2f}")
        
        print(f"\nRisk Metrics:")
        print(f"  Volatility:        {metrics['volatility_pct']:.2f}%")
        print(f"  Max Drawdown:      {metrics['max_drawdown_pct']:.2f}%")
        
        print(f"\nTrading:")
        print(f"  Trading Days:      {metrics['trading_days']}")
        
        if trades:
            winners = [t for t in trades if t.pnl > 0]
            losers = [t for t in trades if t.pnl < 0]
            
            total_trades = len([t for t in trades if t.side == 'SELL'])
            win_rate = len(winners) / total_trades * 100 if total_trades > 0 else 0
            
            gross_profit = sum(t.pnl for t in winners)
            gross_loss = abs(sum(t.pnl for t in losers))
            profit_factor = gross_profit / gross_loss if gross_loss > 0 else float('inf')
            
            print(f"  Total Trades:      {total_trades}")
            print(f"  Win Rate:          {win_rate:.1f}%")
            print(f"  Profit Factor:     {profit_factor:.2f}")
        
        print("\n" + "="*60)
```

**Tasks:**
- [ ] Implement PerformanceTracker class
- [ ] Implement all metric calculations
- [ ] Add formatted report printing
- [ ] Write unit tests with known values
- [ ] Verify calculations match expected results

**Deliverable:** Complete performance reporting

---

### Step 7.2: Create Visualization Module
**Time:** Day 3-5

**File: `src/performance/visualization.py`**
```python
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib.dates as mdates
from typing import List, Optional

def plot_equity_curve(equity_df: pd.DataFrame, 
                     title: str = "Equity Curve",
                     save_path: Optional[str] = None) -> None:
    """Plot equity curve over time"""
    fig, ax = plt.subplots(figsize=(12, 6))
    
    ax.plot(equity_df['timestamp'], equity_df['equity'], 
            linewidth=1.5, color='blue')
    
    ax.set_title(title, fontsize=14)
    ax.set_xlabel('Date')
    ax.set_ylabel('Portfolio Value ($)')
    ax.grid(True, alpha=0.3)
    
    # Format x-axis dates
    ax.xaxis.set_major_formatter(mdates.DateFormatter('%Y-%m'))
    ax.xaxis.set_major_locator(mdates.MonthLocator(interval=3))
    plt.xticks(rotation=45)
    
    plt.tight_layout()
    
    if save_path:
        plt.savefig(save_path, dpi=150)
    plt.show()

def plot_drawdown(equity_df: pd.DataFrame,
                 save_path: Optional[str] = None) -> None:
    """Plot drawdown over time"""
    equity = equity_df['equity']
    running_max = equity.cummax()
    drawdown = (equity - running_max) / running_max * 100
    
    fig, ax = plt.subplots(figsize=(12, 4))
    
    ax.fill_between(equity_df['timestamp'], drawdown, 0, 
                    alpha=0.3, color='red')
    ax.plot(equity_df['timestamp'], drawdown, 
            linewidth=1, color='red')
    
    ax.set_title('Drawdown', fontsize=14)
    ax.set_xlabel('Date')
    ax.set_ylabel('Drawdown (%)')
    ax.grid(True, alpha=0.3)
    
    ax.xaxis.set_major_formatter(mdates.DateFormatter('%Y-%m'))
    plt.xticks(rotation=45)
    
    plt.tight_layout()
    
    if save_path:
        plt.savefig(save_path, dpi=150)
    plt.show()

def plot_returns_distribution(equity_df: pd.DataFrame,
                             save_path: Optional[str] = None) -> None:
    """Plot distribution of daily returns"""
    returns = equity_df['equity'].pct_change().dropna() * 100
    
    fig, ax = plt.subplots(figsize=(10, 6))
    
    ax.hist(returns, bins=50, edgecolor='black', alpha=0.7)
    ax.axvline(returns.mean(), color='red', linestyle='--', 
               label=f'Mean: {returns.mean():.2f}%')
    ax.axvline(0, color='black', linestyle='-', alpha=0.5)
    
    ax.set_title('Daily Returns Distribution', fontsize=14)
    ax.set_xlabel('Daily Return (%)')
    ax.set_ylabel('Frequency')
    ax.legend()
    ax.grid(True, alpha=0.3)
    
    plt.tight_layout()
    
    if save_path:
        plt.savefig(save_path, dpi=150)
    plt.show()

def create_full_report(equity_df: pd.DataFrame,
                      output_dir: str = "output") -> None:
    """Generate full visual report"""
    import os
    os.makedirs(output_dir, exist_ok=True)
    
    plot_equity_curve(equity_df, 
                     save_path=f"{output_dir}/equity_curve.png")
    plot_drawdown(equity_df,
                 save_path=f"{output_dir}/drawdown.png")
    plot_returns_distribution(equity_df,
                             save_path=f"{output_dir}/returns_dist.png")
    
    print(f"Report saved to {output_dir}/")
```

**Tasks:**
- [ ] Implement equity curve plotting
- [ ] Implement drawdown plotting
- [ ] Implement returns distribution
- [ ] Add save functionality
- [ ] Test with sample data

**Deliverable:** Visualization module

---

## Week 8: Integration & Testing

### Step 8.1: Create Main Run Script
**Time:** Day 1-2

**File: `scripts/run_backtest.py`**
```python
#!/usr/bin/env python
"""
Main script to run backtests.

Usage:
    python scripts/run_backtest.py --config configs/backtest_config.yaml
"""
import argparse
from pathlib import Path
import sys

# Add src to path
sys.path.insert(0, str(Path(__file__).parent.parent))

from src.config import load_config
from src.data.data_handler import HistoricalCSVDataHandler
from src.strategy.sma_crossover import SMACrossoverStrategy
from src.portfolio.portfolio import Portfolio
from src.execution.execution_handler import ExecutionHandler
from src.performance.metrics import PerformanceTracker
from src.performance.visualization import create_full_report
from src.backtest.engine import BacktestEngine

def main():
    parser = argparse.ArgumentParser(description='Run backtest')
    parser.add_argument('--config', required=True, 
                       help='Path to config file')
    parser.add_argument('--output', default='output',
                       help='Output directory for reports')
    args = parser.parse_args()
    
    # Load configuration
    print(f"Loading config from {args.config}")
    config = load_config(args.config)
    
    # Initialize components
    print("\nInitializing components...")
    
    data_handler = HistoricalCSVDataHandler(
        data_path=config.data.data_path,
        symbols=config.data.symbols,
        start_date=config.data.start_date,
        end_date=config.data.end_date
    )
    
    strategy = SMACrossoverStrategy(
        symbols=config.data.symbols,
        parameters=config.strategy.parameters
    )
    
    portfolio = Portfolio(
        initial_capital=config.execution.initial_capital,
        symbols=config.data.symbols,
        commission_pct=config.execution.commission_pct
    )
    
    execution_handler = ExecutionHandler(
        commission_pct=config.execution.commission_pct,
        slippage_pct=config.execution.slippage_pct
    )
    
    performance_tracker = PerformanceTracker(
        initial_capital=config.execution.initial_capital
    )
    
    # Create and run backtest engine
    engine = BacktestEngine(
        data_handler=data_handler,
        strategy=strategy,
        portfolio=portfolio,
        execution_handler=execution_handler,
        performance_tracker=performance_tracker
    )
    
    results = engine.run()
    
    # Print performance report
    performance_tracker.print_report(trades=results['trade_history'])
    
    # Generate visual reports
    print("\nGenerating visual reports...")
    equity_curve = performance_tracker.get_equity_curve()
    create_full_report(equity_curve, output_dir=args.output)
    
    # Print execution statistics
    exec_stats = execution_handler.get_statistics()
    print(f"\nExecution Statistics:")
    print(f"  Orders Processed:   {exec_stats['orders_processed']}")
    print(f"  Total Commission:   ${exec_stats['total_commission']:.2f}")
    print(f"  Total Slippage:     ${exec_stats['total_slippage_cost']:.2f}")
    
    return results

if __name__ == '__main__':
    main()
```

**Tasks:**
- [ ] Create main run script
- [ ] Wire up all components
- [ ] Add command-line arguments
- [ ] Test end-to-end execution

**Deliverable:** Working backtest runner

---

### Step 8.2: Write Integration Tests
**Time:** Day 2-4

**File: `tests/test_integration.py`**
```python
import pytest
import pandas as pd
import numpy as np
from datetime import datetime
from pathlib import Path

# Test fixtures and integration tests

@pytest.fixture
def sample_data(tmp_path):
    """Create sample CSV data for testing"""
    dates = pd.date_range('2022-01-01', '2022-12-31', freq='D')
    
    # Create trending data (should trigger signals)
    np.random.seed(42)
    prices = 100 + np.cumsum(np.random.randn(len(dates)) * 0.5)
    
    df = pd.DataFrame({
        'date': dates,
        'open': prices * 0.99,
        'high': prices * 1.01,
        'low': prices * 0.98,
        'close': prices,
        'volume': np.random.randint(1000000, 5000000, len(dates))
    })
    
    data_dir = tmp_path / "data"
    data_dir.mkdir()
    df.to_csv(data_dir / "TEST.csv", index=False)
    
    return str(data_dir)

def test_full_backtest_runs(sample_data):
    """Test that full backtest completes without errors"""
    from src.data.data_handler import HistoricalCSVDataHandler
    from src.strategy.sma_crossover import SMACrossoverStrategy
    from src.portfolio.portfolio import Portfolio
    from src.execution.execution_handler import ExecutionHandler
    from src.performance.metrics import PerformanceTracker
    from src.backtest.engine import BacktestEngine
    
    symbols = ['TEST']
    
    data_handler = HistoricalCSVDataHandler(
        data_path=sample_data,
        symbols=symbols,
        start_date='2022-01-01',
        end_date='2022-12-31'
    )
    
    strategy = SMACrossoverStrategy(
        symbols=symbols,
        parameters={'short_window': 10, 'long_window': 30}
    )
    
    portfolio = Portfolio(
        initial_capital=100000,
        symbols=symbols
    )
    
    execution_handler = ExecutionHandler()
    performance_tracker = PerformanceTracker(initial_capital=100000)
    
    engine = BacktestEngine(
        data_handler=data_handler,
        strategy=strategy,
        portfolio=portfolio,
        execution_handler=execution_handler,
        performance_tracker=performance_tracker
    )
    
    results = engine.run()
    
    # Verify results structure
    assert 'bars_processed' in results
    assert 'final_equity' in results
    assert results['bars_processed'] > 0
    assert results['final_equity'] > 0

def test_no_look_ahead_bias(sample_data):
    """Verify data handler prevents look-ahead bias"""
    from src.data.data_handler import HistoricalCSVDataHandler
    
    handler = HistoricalCSVDataHandler(
        data_path=sample_data,
        symbols=['TEST'],
        start_date='2022-01-01',
        end_date='2022-12-31'
    )
    
    # At start, should only have first bar
    bars = handler.get_latest_bars('TEST', 100)
    assert len(bars) == 1
    
    # Advance 10 bars
    for _ in range(10):
        handler.update_bars()
    
    # Should now have 11 bars (0-10 inclusive)
    bars = handler.get_latest_bars('TEST', 100)
    assert len(bars) == 11

def test_pnl_calculation():
    """Verify P&L calculations are correct"""
    from src.portfolio.portfolio import Portfolio
    from src.events.event import FillEvent, OrderSide
    
    portfolio = Portfolio(
        initial_capital=100000,
        symbols=['TEST']
    )
    
    # Simulate buy
    fill = FillEvent(
        timestamp=datetime.now(),
        symbol='TEST',
        side=OrderSide.BUY,
        quantity=100,
        fill_price=50.0,
        commission=5.0
    )
    portfolio.on_fill(fill)
    
    assert portfolio.positions['TEST'].quantity == 100
    assert portfolio.positions['TEST'].avg_cost == 50.0
    assert portfolio.cash == 100000 - (100 * 50 + 5)  # 94995
    
    # Simulate sell at profit
    portfolio.current_prices['TEST'] = 55.0
    fill = FillEvent(
        timestamp=datetime.now(),
        symbol='TEST',
        side=OrderSide.SELL,
        quantity=100,
        fill_price=55.0,
        commission=5.5
    )
    portfolio.on_fill(fill)
    
    assert portfolio.positions['TEST'].quantity == 0
    # P&L = (55 - 50) * 100 - 5.5 = 494.5
    assert abs(portfolio.positions['TEST'].realized_pnl - 494.5) < 0.01
```

**Tasks:**
- [ ] Write integration tests
- [ ] Write unit tests for each component
- [ ] Test edge cases (no trades, all losses, etc.)
- [ ] Verify P&L calculations manually
- [ ] Run full test suite

**Deliverable:** Comprehensive test suite

---

### Step 8.3: Documentation & Final Validation
**Time:** Day 4-5

**Tasks:**
- [ ] Write README.md with setup instructions
- [ ] Document configuration options
- [ ] Create example notebooks
- [ ] Run backtest on multiple symbols
- [ ] Compare results with manual calculations
- [ ] Final code review and cleanup

**File: `README.md`**
```markdown
# Trading System MVP

Event-driven backtesting engine for systematic trading strategies.

## Quick Start

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Download sample data:
   ```bash
   python scripts/download_data.py --symbols AAPL MSFT --start 2020-01-01
   ```

3. Run backtest:
   ```bash
   python scripts/run_backtest.py --config configs/backtest_config.yaml
   ```

## Project Structure

- `src/` - Core library code
- `tests/` - Unit and integration tests
- `configs/` - Configuration files
- `scripts/` - Utility scripts
- `data/` - Market data (not in git)
- `output/` - Backtest results

## Configuration

See `configs/backtest_config.yaml` for available options.

## Adding New Strategies

1. Create new file in `src/strategy/`
2. Inherit from `Strategy` base class
3. Implement `calculate_signals()` method
```

**Deliverable:** Complete documentation

---

## Success Criteria Checklist

### Functional Requirements
- [ ] Backtest runs without errors on sample data
- [ ] Events process in correct order (Market → Signal → Order → Fill)
- [ ] P&L calculations match manual verification
- [ ] No look-ahead bias (verified by test)
- [ ] Performance metrics calculate correctly (verified against known values)

### Code Quality
- [ ] All unit tests pass
- [ ] Integration tests pass
- [ ] Code passes linting (ruff, black)
- [ ] Type hints present on public methods
- [ ] Docstrings on all classes and public methods

### Documentation
- [ ] README with setup instructions
- [ ] Configuration documented
- [ ] Example usage provided

---

## Risk Mitigation

| Risk | Mitigation | Status |
|------|------------|--------|
| Look-ahead bias | Structural prevention in DataHandler | ☐ |
| Incorrect P&L | Manual verification + unit tests | ☐ |
| Performance issues | Profile if >1min for 1yr data | ☐ |
| Configuration errors | Pydantic validation | ☐ |

---

## Next Steps (Phase 2 Preview)

After completing Phase 1:
1. Add multi-asset support to Portfolio
2. Implement position sizing algorithms
3. Add basic risk limits
4. Implement mean-variance optimization

---

*Phase 1 Development Plan v1.0*